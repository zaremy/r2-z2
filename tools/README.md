# tools/

Project-side utilities. Empty by design — nothing here yet that isn't better
placed in `mac-prototype/`.

The Mac probe launcher lives at `../mac-prototype/r2`; the ad-hoc-signed
`R2Probe.app` it builds is gitignored and regenerated with `./r2 --rebuild`.
